import { Router } from 'express';import bcrypt from 'bcryptjs';import { z } from 'zod';import { Prisma,prisma,SchoolStatus } from '@nexora/database';import { requireAuth,requireRoles } from '../middleware/auth.js';import { routeParam } from '../utils/http.js';
const router=Router();router.use(requireAuth,requireRoles('SUPER_ADMIN'));
router.get('/dashboard',async(_req,res)=>{const now=new Date(),monthStart=new Date(now.getFullYear(),now.getMonth(),1),sixMonthsAgo=new Date(now.getFullYear(),now.getMonth()-5,1);const[schools,users,activeSchools,attentionSchools,newSchools,recentSchools,studentCount,teacherCount,storage]=await Promise.all([prisma.school.count(),prisma.user.count({where:{role:{in:['STUDENT','TEACHER','STAFF','PRINCIPAL','PARENT']}}}),prisma.school.count({where:{status:'ACTIVE'}}),prisma.school.count({where:{status:{in:['TRIAL','GRACE_PERIOD','READ_ONLY','SUSPENDED']}}}),prisma.school.count({where:{createdAt:{gte:monthStart}}}),prisma.school.findMany({where:{createdAt:{gte:sixMonthsAgo}},select:{createdAt:true}}),prisma.user.count({where:{role:'STUDENT'}}),prisma.user.count({where:{role:'TEACHER'}}),prisma.$queryRaw<any[]>(Prisma.sql`SELECT COALESCE(SUM("sizeBytes"),0) bytes,COUNT(*) files FROM "StorageObject"`)]);const monthly=Array.from({length:6},(_,i)=>{const date=new Date(now.getFullYear(),now.getMonth()-5+i,1),next=new Date(date.getFullYear(),date.getMonth()+1,1);return{key:`${date.getFullYear()}-${date.getMonth()}`,month:date.toLocaleString('en',{month:'short'}),schools:recentSchools.filter(s=>s.createdAt>=date&&s.createdAt<next).length}});res.json({metrics:{schools,activeSchools,students:studentCount,teachers:teacherCount,users,attentionSchools,newSchools,storageBytes:Number(storage[0]?.bytes??0),storageFiles:Number(storage[0]?.files??0)},trend:monthly})});
const schoolSchema=z.object({name:z.string().min(2),slug:z.string().min(2).regex(/^[a-z0-9-]+$/),email:z.string().email().optional(),phone:z.string().optional(),address:z.string().optional(),description:z.string().optional(),logoUrl:z.string().url().optional(),studentLimit:z.number().int().positive().default(1000),teacherLimit:z.number().int().positive().default(100),storageLimitMb:z.number().int().positive().default(1024),principal:z.object({firstName:z.string().min(2),lastName:z.string().min(1),email:z.string().email(),username:z.string().min(3),password:z.string().min(8)})});
router.get('/schools',async(_req,res)=>{const schools=await prisma.school.findMany({orderBy:{createdAt:'desc'},include:{_count:{select:{users:true,classes:true,exams:true}},users:{where:{role:'PRINCIPAL'},select:{id:true,firstName:true,lastName:true,email:true,isActive:true}}}}),storage=await prisma.$queryRaw<any[]>(Prisma.sql`SELECT "schoolId",COALESCE(SUM("sizeBytes"),0) bytes,COUNT(*) files FROM "StorageObject" GROUP BY "schoolId"`),usage=new Map(storage.map(x=>[x.schoolId,{storageUsedBytes:Number(x.bytes??0),storageFiles:Number(x.files??0)}]));res.json(schools.map(s=>({...s,...(usage.get(s.id)??{storageUsedBytes:0,storageFiles:0})})))});
router.post('/schools',async(req,res)=>{const parsed=schoolSchema.safeParse(req.body);if(!parsed.success)return res.status(400).json({message:'Invalid school data',issues:parsed.error.flatten()});const{principal,...schoolData}=parsed.data,passwordHash=await bcrypt.hash(principal.password,12),school=await prisma.$transaction(async tx=>{const created=await tx.school.create({data:schoolData}),principalUser=await tx.user.create({data:{schoolId:created.id,role:'PRINCIPAL',email:principal.email.toLowerCase(),username:principal.username,passwordHash,firstName:principal.firstName,lastName:principal.lastName}});await tx.auditLog.create({data:{schoolId:created.id,actorId:req.auth!.userId,action:'SCHOOL_CREATED',entityType:'School',entityId:created.id,afterData:{name:created.name,principalId:principalUser.id}}});return created});res.status(201).json(school)});
const lifecycleSchema=z.object({status:z.nativeEnum(SchoolStatus),subscriptionEnd:z.coerce.date().optional(),graceEndsAt:z.coerce.date().optional(),reason:z.string().max(500).optional()});router.patch('/schools/:schoolId/status',async(req,res)=>{const parsed=lifecycleSchema.safeParse(req.body);if(!parsed.success)return res.status(400).json({message:'Invalid lifecycle update'});const schoolId=routeParam(req.params.schoolId),before=await prisma.school.findUnique({where:{id:schoolId}});if(!before)return res.status(404).json({message:'School not found'});const updated=await prisma.$transaction(async tx=>{const school=await tx.school.update({where:{id:before.id},data:{status:parsed.data.status,subscriptionEnd:parsed.data.subscriptionEnd,graceEndsAt:parsed.data.graceEndsAt}});await tx.auditLog.create({data:{schoolId:school.id,actorId:req.auth!.userId,action:'SCHOOL_STATUS_CHANGED',entityType:'School',entityId:school.id,beforeData:{status:before.status},afterData:{status:school.status,reason:parsed.data.reason??null}}});return school});res.json(updated)});
const quotaSchema=z.object({studentLimit:z.coerce.number().int().min(1).max(1000000),teacherLimit:z.coerce.number().int().min(1).max(100000),storageLimitMb:z.coerce.number().int().min(100).max(1024*1024)});router.patch('/schools/:schoolId/quotas',async(req,res)=>{const p=quotaSchema.safeParse(req.body);if(!p.success)return res.status(400).json({message:'Invalid school quotas'});const id=routeParam(req.params.schoolId),before=await prisma.school.findUnique({where:{id}});if(!before)return res.status(404).json({message:'School not found'});const usage=await prisma.$queryRaw<any[]>(Prisma.sql`SELECT COALESCE(SUM("sizeBytes"),0) bytes FROM "StorageObject" WHERE "schoolId"=${id}`),used=Number(usage[0]?.bytes??0),limitBytes=p.data.storageLimitMb*1024*1024;if(limitBytes<used)return res.status(409).json({message:'Storage quota cannot be lower than current usage',usedBytes:used});const updated=await prisma.$transaction(async tx=>{const school=await tx.school.update({where:{id},data:p.data});await tx.auditLog.create({data:{schoolId:id,actorId:req.auth!.userId,action:'SCHOOL_QUOTAS_CHANGED',entityType:'School',entityId:id,beforeData:{studentLimit:before.studentLimit,teacherLimit:before.teacherLimit,storageLimitMb:before.storageLimitMb},afterData:p.data}});return school});res.json({...updated,storageUsedBytes:used})});export default router;