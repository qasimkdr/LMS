import { Router } from 'express';import { Prisma,prisma } from '@nexora/database';import { requireAuth,requireRoles,requireTenant } from '../middleware/auth.js';
const router=Router();router.use(requireAuth,requireTenant,requireRoles('PRINCIPAL'));
router.get('/export',async(req,res)=>{const schoolId=req.auth!.schoolId!,school=await prisma.school.findUnique({where:{id:schoolId}});if(!school)return res.status(404).json({message:'School not found'});const [users,classes,subjects,teacherAssignments,approvalPolicies,approvalRequests,attendanceSessions,exams,examAttempts,announcements,assignments,materials,syllabusItems,notifications,academicTerms,timetableEntries,leaveRequests,feeStructures,feePayments,feeBatches,feeAdjustments,schoolEvents,storageObjects,reportCards]=await Promise.all([prisma.user.findMany({where:{schoolId},select:{id:true,schoolId:true,role:true,email:true,username:true,firstName:true,lastName:true,avatarUrl:true,isActive:true,lastLoginAt:true,createdAt:true,updatedAt:true}}),prisma.class.findMany({where:{schoolId}}),prisma.subject.findMany({where:{schoolId}}),prisma.teacherAssignment.findMany({where:{schoolId}}),prisma.approvalPolicy.findMany({where:{schoolId}}),prisma.approvalRequest.findMany({where:{schoolId},include:{versions:true}}),prisma.attendanceSession.findMany({where:{schoolId},include:{records:true}}),prisma.exam.findMany({where:{schoolId},include:{questions:{include:{options:true}}}}),prisma.examAttempt.findMany({where:{schoolId},include:{answers:true}}),prisma.announcement.findMany({where:{schoolId}}),prisma.assignment.findMany({where:{schoolId},include:{submissions:true}}),prisma.courseMaterial.findMany({where:{schoolId}}),prisma.syllabusItem.findMany({where:{schoolId}}),prisma.notification.findMany({where:{schoolId}}),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "AcademicTerm" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "TimetableEntry" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "LeaveRequest" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "FeeStructure" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "FeePayment" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "FeeRecoveryBatch" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "FeeAdjustment" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "SchoolEvent" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "StorageObject" WHERE "schoolId"=${schoolId}`),prisma.$queryRaw<any[]>(Prisma.sql`SELECT * FROM "ReportCardPublication" WHERE "schoolId"=${schoolId}`)]);const students=await prisma.studentProfile.findMany({where:{schoolId},include:{parentLinks:true}}),parents=await prisma.parentProfile.findMany({where:{schoolId},include:{students:true}});const backup={format:'nexora-school-backup',version:1,exportedAt:new Date().toISOString(),school:{...school},data:{users,students,parents,classes,subjects,teacherAssignments,approvalPolicies,approvalRequests,attendanceSessions,exams,examAttempts,announcements,assignments,materials,syllabusItems,notifications,academicTerms,timetableEntries,leaveRequests,feeStructures,feePayments,feeBatches,feeAdjustments,schoolEvents,storageObjects,reportCards}};await prisma.auditLog.create({data:{schoolId,actorId:req.auth!.userId,action:'SCHOOL_BACKUP_EXPORTED',entityType:'School',entityId:schoolId,afterData:{format:backup.format,version:backup.version,exportedAt:backup.exportedAt}}});res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Content-Disposition',`attachment; filename="nexora-${school.slug}-${new Date().toISOString().slice(0,10)}.json"`);res.send(JSON.stringify(backup,null,2))});export default router;
