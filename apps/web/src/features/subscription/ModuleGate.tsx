import LockRoundedIcon from '@mui/icons-material/LockRounded';
import { Skeleton } from '@mui/material';
import type { ReactNode } from 'react';
import { useSubscription,type ModuleKey } from './SubscriptionProvider';

export default function ModuleGate({module,children}:{module:ModuleKey;children:ReactNode}){const{loading,hasModule,subscription}=useSubscription();if(loading)return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-4xl"><Skeleton height={170} variant="rounded" sx={{borderRadius:6}}/><Skeleton className="mt-4" height={320} variant="rounded" sx={{borderRadius:6}}/></div></main>;if(hasModule(module))return <>{children}</>;return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#eef4ff,transparent_40%),#f8fafc] p-5"><section className="w-full max-w-xl rounded-[34px] border border-white bg-white/90 p-8 text-center shadow-[0_30px_90px_rgba(70,90,140,.16)]"><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-gradient-to-br from-indigo-600 to-violet-600 text-white"><LockRoundedIcon/></div><p className="mt-5 text-xs font-black uppercase tracking-[.2em] text-indigo-600">Plan entitlement</p><h1 className="mt-2 text-3xl font-black text-slate-950">{module.replaceAll('_',' ')} is not enabled</h1><p className="mt-3 text-sm leading-6 text-slate-500">This school is currently on the <b>{subscription?.planName||subscription?.planCode||'assigned'}</b> plan. Ask your Principal or Nexora platform administrator to enable this module.</p></section></main>}
